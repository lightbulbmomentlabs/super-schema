/**
 * Refinement Configuration
 *
 * Maximum number of AI refinements allowed per schema generation.
 * Change this value to adjust the refinement limit globally.
 */
export const MAX_REFINEMENTS = 2
